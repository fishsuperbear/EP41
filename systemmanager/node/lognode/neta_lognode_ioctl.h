#ifndef NETA_LOGNODE_IOCTL_H
#define NETA_LOGNODE_IOCTL_H

#include "logblockreader.h"
// shuold be exactly the same file as file used by lognode user impl
#include "neta_lognode_ioctl_common.h"
// shuold be exactly the same file as file used by lognode user impl
#include "neta_lognode_ioctl_writer.h"
// shuold be exactly the same file as file used by lognode user impl
#include "neta_lognode_ioctl_reader.h"

#endif
